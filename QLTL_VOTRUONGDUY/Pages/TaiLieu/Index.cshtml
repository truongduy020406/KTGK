@page
@model QLTL_VOTRUONGDUY.Pages.TaiLieu.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TaiLieu[0].TenTaiLieu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaiLieu[0].Mota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaiLieu[0].Loai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaiLieu[0].Soluong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TaiLieu) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenTaiLieu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Soluong)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
